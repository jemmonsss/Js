# ---- Core site ----
# Primary username everywhere: J_emmons_07
# "jemmonsss" is ONLY the GitHub handle / org (github.com/jemmonsss).
title: J_emmons_07
description: Links, projects, and free/public web tools by J_emmons_07.
author: J_emmons_07
url: "https://jemmonsss.github.io"
baseurl: "/Js"                  # "" for a user/org site, "/repo" for project site
lang: en-US
timezone: UTC

# ---- Build ----
markdown: kramdown
encoding: utf-8
highlighter: rouge              # syntax highlighting
excerpt_separator: "\n\n"
permalink: pretty

# ---- Collections: Projects gallery ----
collections:
  projects:
    output: true                # generate a page per project
    permalink: /projects/:slug/

# ---- Plugins (GitHub-Pages safe only) ----
plugins:
  - jekyll-seo-tag              # automated metadata / OG tags
  - jekyll-feed                 # RSS (metadata extraction)
  - jekyll-sitemap              # SEO
  - jekyll-redirect-from        # future-proofing

# Items excluded from sitemap.xml (404 / drafts)
sitemap:
  exclude:
    - /404.html
    - /drafts/
# NOTE: rouge is built into Jekyll; no plugin entry needed.
# Avoid non-allowlisted gems (e.g. jekyll-archives) or Pages build fails.

# ---- SEO / social (consumed by jekyll-seo-tag) ----
# NOTE: "jemmonsss" is the GitHub handle only. Primary username is J_emmons_07.
social:
  name: J_emmons_07
  links:
    - https://github.com/jemmonsss
    - https://www.youtube.com/@J_emmons_07
    - https://www.tiktok.com/@J_emmons_07

# jekyll-seo-tag site-wide defaults
twitter:
  username: jemmonsss
  card: summary_large_image
logo: /images/logo.png
og_image: /images/logo.png

# Defaults applied automatically (less front-matter repetition)
# No `image` fallback: project cards use a live <iframe> preview of the
# actual site when no screenshot is provided.
defaults:
  - scope:
      path: ""
      type: projects
    values:
      layout: default

# ---- Theme / assets ----
sass:
  style: compressed             # minify CSS in production
include:
  - _projects
exclude:
  - Gemfile
  - Gemfile.lock
  - README.md
  - vendor
  - templates                   # authoring templates, not published
